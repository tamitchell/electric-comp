import React from "react";
import { Container, Row, Col } from "reactstrap";

const Hero = () => {
  return <header>
      <Container fluid={true}>
      <Row>
          
      </Row>
      </Container>
  </header>;
};

export default Hero;
